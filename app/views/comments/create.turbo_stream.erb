<%= turbo_stream.append "#{dom_id(@tweet)}_comments" do %>
  <%= render 'comment', comment: @comment %>
<% end %>

<%= turbo_stream.update "#{dom_id(@tweet)}_comments_count" do %>
  <%= render 'tweets/comments_count', tweet: @tweet %>
<% end %>